# ──────────────────────────────────────────────────────────────────────────────
# requirements-dev.txt
# Development-only dependencies — NOT needed to run the dashboard.
# Install with: pip install -r requirements-dev.txt
# ──────────────────────────────────────────────────────────────────────────────

# Notebooks
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0

# Linting & code style
flake8>=7.0.0
flake8-bugbear>=24.0.0     # catches common Python anti-patterns
pycodestyle>=2.11.0

# Type checking (optional)
mypy>=1.0.0

# Testing (for future unit tests)
pytest>=8.0.0
pytest-cov>=5.0.0
